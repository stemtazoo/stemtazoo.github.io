---
layout: page
title: 状態遷移表とは？入力を順番に追って最後の状態を求める方法【基本情報技術者試験】
description: 状態遷移表の読み方を、現在状態・入力・次状態の関係から整理します。空欄の扱い、文字列検査、不合格状態の追い方、状態遷移図との違いを確認し、科目Aで選択肢を切る判断軸を身につけます。
permalink: /fe/state-transition-table/
tags: [fe, technology, basic-theory, state-transition]
fe_section: テクノロジ系
fe_subsection: 基礎理論
last_modified_at: 2026-08-05
---

## まず結論

**状態遷移表**は、現在の状態と入力の組合せから、次の状態を確認するための表です。

科目Aでは、次の順で追えば解けます。

```text
現在状態を確認する
↓
入力との交点を見る
↓
次の状態へ更新する
↓
次の入力を処理する
```

一番大切なのは、**状態が変わったら、次の入力は新しい状態から読む**ことです。

文字列検査では、文字を左から1文字ずつ読み、数字・符号・小数点・空白などに分類して遷移を追います。

## 直感的な説明

状態遷移表は、機械やシステムの「動作ルール表」と考えると分かりやすいです。

たとえば、自動販売機を考えます。

- 待機中に硬貨が入る
- 商品選択待ちへ移る
- 商品ボタンが押される
- 商品を出して待機中へ戻る

このように、システムは今いる状態と入力によって次の状態を変えます。

```text
現在状態 ＋ 入力 → 次状態
```

状態遷移表では、この関係を行と列の交点で表します。

文字列検査も同じ考え方です。

```text
現在状態
＋
次の1文字の種類
→
次状態
```

文字列全体をまとめて判断するのではなく、左から1文字ずつ処理します。

## 定義・仕組み

状態遷移表には、主に次の3つがあります。

| 項目 | 意味 |
|---|---|
| 現在状態 | 今いる状態 |
| 入力 | 今回与えられる信号やイベント |
| 次状態 | 入力後に移る状態 |

たとえば、次のような表があるとします。

| 入力＼状態 | S1 | S2 | S3 |
|---|---|---|---|
| t1 | S2 |  | S1 |
| t2 |  | S3 | S2 |

現在状態がS1で、入力がt1なら、交点はS2です。

```text
S1 --t1--> S2
```

次の入力を処理するときは、S1ではなくS2から読みます。

### 空欄の扱い

問題文で「空欄は状態が変化しない」と指定されている場合、現在状態をそのまま維持します。

```text
現在状態：S2
入力：t1
交点：空欄

→ S2のまま
```

空欄を「処理終了」や「未定義」と決めつけないようにします。空欄の意味は、必ず問題文の条件を確認します。

### 不合格状態・エラー状態

文字列検査では、特定の状態へ入ると不合格になる場合があります。

```text
状態 e に入る
→ 不合格
```

この場合、一度でもeへ遷移した選択肢は不合格です。

最後の文字まで読む必要がない場合もあります。

## 科目Aでどう出る？

状態遷移表の問題では、複数の入力を順番に与え、最後の状態を求める形式がよく出ます。

次の作業表を自分で書くと、途中の見落としを防げます。

| 手順 | 現在状態 | 入力 | 次状態 |
|---:|---|---|---|
| 1 | S1 | t1 | S2 |
| 2 | S2 | t2 | S3 |
| 3 | S3 | t1 | S1 |

解く手順は次のとおりです。

1. 初期状態を書く
2. 入力を1つだけ読む
3. 表の交点を確認する
4. 現在状態を次状態へ書き換える
5. 次の入力へ進む

### 文字列を検査する問題

文字列検査では、文字を左から1文字ずつ読みます。

```text
文字そのものを見る
↓
数字・符号・小数点・空白などに分類する
↓
現在状態との交点を見る
↓
次状態へ更新する
```

例えば、次のように記録します。

```text
+0010：a → c → b → b → b → b
-1   ：a → c → b
12.2 ：a → b → b → d → e
9.空白：a → b → d → a
```

不合格状態がeなら、eを含む経路だけを探せば判断できます。

文字列検査の手順は次のとおりです。

1. 初期状態を書く
2. 次の1文字を確認する
3. その文字を数字・符号・小数点・空白などに分類する
4. 現在状態と文字種類の交点を見る
5. 状態を更新する
6. 不合格状態へ入ったら、その選択肢は不合格と判断する

### 状態の意味を推測する

状態名がa、b、cのような記号でも、遷移から意味を推測できます。

例えば、次のように考えます。

| 状態 | 推測できる意味の例 |
|---|---|
| a | 初期状態、または入力前の状態 |
| b | 数字を読んでいる状態 |
| c | 符号を読んだ状態 |
| d | 小数点を読んだ直後 |
| e | 不合格状態 |

ただし、状態の意味は補助的な理解です。

最終的な判断は、必ず問題文の状態遷移表に従います。

### 判断表

| 問題文の表現 | 見るポイント |
|---|---|
| 現在の状態がS1 | 最初に見る列はS1 |
| 入力を順番に与える | 1つずつ処理する |
| 文字列を左端から検査 | 左から1文字ずつ読む |
| 数字・符号・小数点・空白 | 文字を種類へ分類する |
| 状態eなら不合格 | eへ一度でも入るかを見る |
| 空欄は変化なし | 現在状態を維持する |
| 最後の状態を求める | 全入力を処理した後の状態を見る |
| 行が入力、列が状態 | 見出しを確認して交点を読む |

## どんな場面で使う？

状態遷移の考え方は、状態によって動作が変わるシステムで使われます。

たとえば、次のような場面です。

- 自動販売機
- エレベーター
- 信号機
- ログイン処理
- 通信プロトコル
- 組込み機器の制御
- ゲームキャラクターの動作
- 入力文字列の形式検査
- 数値や識別番号の入力チェック

同じ入力でも、現在状態が違えば次の動作が変わることがあります。

```text
入力が同じ
≠
結果も同じ
```

現在状態と入力をセットで見ることが重要です。

文字列検査では、同じ「数字」でも現在状態によって結果が変わります。

```text
符号の後の数字
→ 正常な遷移

不正な位置にある数字
→ 不合格状態へ遷移
```

## よくある誤解・混同

### 最初の状態の列を最後まで見る

一度状態が変わった後も、最初の状態の列を見続ける誤りです。

```text
S1 → S3
```

となった後は、次の入力をS3列で確認します。

> 状態が変わるたびに、見る列も変わります。

### 文字列をまとめて判断する

`12.2`を「小数だから正しい」とまとめて判断するのは誤りです。

```text
1
2
.
2
```

のように、1文字ずつ表へ当てはめます。

### 現実の書式を優先する

一般的に正しい表記でも、問題の状態遷移表では不合格になることがあります。

```text
普通なら正しい
≠
この状態遷移表でも合格
```

問題文の表を正として判断します。

### 不合格状態へ入った後も追い続ける

問題文で「状態eになれば不合格」と指定されているなら、eへ入った時点でその選択肢は不合格です。

残りの文字を追わなくても判断できます。

### 空欄なら入力を無視する

空欄が「状態変化なし」を表す場合でも、入力は1回分処理されています。

```text
状態はそのまま
↓
次の入力へ進む
```

同じ入力をもう一度読むわけではありません。

### 行と列を逆に読む

表によって、行と列の配置は異なります。

- 行が入力、列が状態
- 行が状態、列が入力

どちらの場合もあります。最初に見出しを確認します。

### 状態遷移表と状態遷移図は別の仕組み

状態遷移表と状態遷移図は、表現方法が違うだけで、表している内容は同じです。

| 表現 | 特徴 |
|---|---|
| 状態遷移表 | 交点から次状態を探しやすい |
| 状態遷移図 | 状態と移動の流れを直感的に見やすい |

どちらも基本は次の関係です。

```text
現在状態 ＋ 入力 → 次状態
```

## まとめ（試験直前用）

- 状態遷移表は、**現在状態と入力から次状態を決める表**
- 入力は**必ず1つずつ順番に処理する**
- 遷移後は、**新しい状態から次の入力を読む**
- 文字列検査では、文字を数字・符号・小数点・空白などに分類する
- 不合格状態へ一度でも入ったら、その選択肢は不合格
- 現実の書式より、問題文の状態遷移表を優先する
- 空欄の意味は問題文を確認し、「変化なし」なら現在状態を維持する
- 行と列の見出しを先に確認する
- 迷ったら、`手順｜現在状態｜入力｜次状態` の作業表を書く

{% include fe_article_footer.html %}
