# SG Example Check Records

This directory stores SG example-question coverage checks and review notes.

Use this directory for temporary or archival review outputs such as `SG例題対応チェック`.
Do not place site operation rules here; keep reusable agent rules in `docs/agent/`.
