# Internal Working Notes

This directory stores internal working notes, review outputs, and agent-generated check records.

Files here are not intended to be published as site content. The directory is excluded from the Jekyll/GitHub Pages build in `_config.yml`.

Current subdirectories:

- `sg-example-checks/`: SG example-question coverage and review check records.
